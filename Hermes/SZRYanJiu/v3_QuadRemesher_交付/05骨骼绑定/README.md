# 05骨骼绑定 — 交付目录

**更新**: 2026-08-28 | 两套方案均已跑通行走动画

## 手写版交付/

| 文件 | 内容 |
|---|---|
| `01_打点模板.blend` | 8个标记点模板（中线黄/右侧红，左侧驱动镜像） |
| `02_骨骼绑定.blend` | 52骨骼，方案A（朝向照抄Mixamo，位置=用户点位），静止姿态0.00°偏差 |
| `03_最终输出.glb` | Godot可导入 |
| `04_行走动画测试.blend` | Mixamo行走，1952/2000顶点变形 |

## ARP版交付/（含手指，优先路线）

| 文件 | 内容 |
|---|---|
| `01_打点模板.blend` | 17点模板（AI预置+官方命名，右侧主点/左侧镜像） |
| `02_骨骼绑定.blend` | **55骨骼含手指30根**，全部落在用户17个标记点上，roll对齐Mixamo，100%权重 |
| `03_行走测试.blend` | Mixamo行走，2793/3000顶点变形，摆臂8.2cm，脚贴地 |

## 技术要点（详见 方案md记录/.../05骨骼绑定/）

- 两套骨架均为纯变形骨架（无控制器），命名对齐Mixamo
- ARP版走"参考骨架提取"路线：ARP的go_detect对T-pose模型会产出A-pose错位变形骨，但参考骨架（66根，位置精确）正确 → 弃变形骨、提取参考骨架重建
- 手指结构：掌骨区归Hand骨（尾到中指指根），Finger1-3只覆盖指节——与Mixamo约定一致
- 行走动画：Blender 5.x Slotted Action，slot必须绑到装fcurves的channelbag，Hips位移曲线已删（原地走）

## 剩余

- ARP版去控制器导出Godot GLB（骨架已无控制器，待导出+验证）
- 眼球绑定同步进ARP版
