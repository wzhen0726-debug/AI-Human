# -*- coding: utf-8 -*-
"""UV v3 收尾: 03记录文档重写 + 根README结论 + 07行 + 复盘数字 + 交付版同步(保留原行尾)"""
import io, os, shutil, filecmp

BASE = r"E:/WangZhen_Project/AI/ShuZiRen/Hermes/SZRYanJiu"
DEMO = os.path.join(BASE, "演示版_终端控制端")
MD = os.path.join(BASE, "方案md记录")
DELV = os.path.join(BASE, "v3_QuadRemesher_交付")

def rd(p):  return io.open(p, encoding="utf-8", newline="").read()

def wr(p, s):
    eol = "\r\n" if "\r\n" in s else "\n"
    io.open(p, "w", encoding="utf-8", newline="").write(s.replace("\r\n", "\n").replace("\n", eol))

def sub(p, pairs):
    s = rd(p)
    for old, new in pairs:
        assert old in s, f"MISS in {os.path.basename(p)}: {old[:60]}"
        s = s.replace(old, new, 1)
    wr(p, s); print("  ✓ 更新", os.path.relpath(p, BASE))

# ---------- 1. 03 记录文档(重写为 v3 最终版) ----------
doc = os.path.join(MD, "v3_QuadRemesher/03自动UV/UV空间浪费检测与参数寻优_20260917.md")
wr(doc, """# 03 自动UV — 大面积浪费检测 + 自适应参数搜索（2026-09-17 v3 定案）

## 问题
原 UV 展开（Smart UV Project 66° + 自带打包）存在**大面积浪费**：左中/左下整块连续空白
（最大空方块 0.238，占地图 5.68%），利用率仅 30.9%。

## 检测指标（每次展开后测量，全部由数据算出）

| 指标 | 算法 |
|---|---|
| 利用率 U | Σ\\|UV面面积\\| / 1.0（逐面 Shoelace 求和） |
| 最大空方块 | 布局栅格化(256²)+2格膨胀封孔 → 最大空方块 DP（归一化边长/面积） |
| 岛统计 | UV连通分量（共享边且两端UV相等）→ 岛数/最大岛面积与边长/中位岛边长 |
| 纹素密度CV | 逐面 (UV面积 / 3D面积) 的变异系数 |

## 判据（自参照三项比值，≤1 通过；**不含任何模型相关常量，换原始模型依然成立**）

| 比值 | 含义 |
|---|---|
| 废料比 = 最大空方块面积 / 最大岛面积 | <1：空块大到**装不下**最大的岛 = 无大面积浪费（越大越浪费） |
| 密度比 = 候选CV / 基准CV | ≤1：不破坏展开算法自身的纹素密度均匀度（保烘焙） |
| 岛数比 = 候选岛数 / 基准岛数 | ≤1：不新增接缝/碎岛（保"少接缝"） |

## 搜索与循环（5 轮预算，跑完择优，任何失败都不阻断）

- **第1轮**：66°（既有"少接缝"定案）+ 自带打包 = 基准（同时给出三项比值的分母）
- **第2轮**：66°+光顺+重打包（验证"重打包"增益）
- **第3轮**：89°+…（探上界）   **第4轮**：(66+89)/2=77.5°+…
- **第5轮**：朝**合法候选的峰值一侧**再二分一探（无合法者才退用原始U峰）
- **异常防护**：单轮展开失败自动跳过；全部方案都不满足约束时，按
  (废料比最小 → 利用率最高) 取"**最好一版**"继续（告警但不失败）；
- **定状态**：用选定参数重跑一次并复测三项比值，若不过则**依次退用次优**（处理运行波动），
  全部不过保留最优一版并告警 —— 每种情况都保证管线拿到可用的 UV。

## 实测（本模型 161,083 面；程序 ~58s = 5轮×~10s + 定状态复跑）

| 轮 | 方案 | U | 最大空方块 | 岛数 | CV | 废料比/密度比/岛数比 | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | 66° 自带打包（基准） | 30.9% | 0.238(5.68%) | 429 | 0.114 | 1.02/1.00/1.00 | ✗ 大面积浪费 |
| **2** | **66°+光顺+重打包（选定）** | **41.6%** | **0.020(0.04%)** | **429** | **0.108** | **0.01/0.94/1.00** | ✓ |
| 3 | 89°+光顺+重打包 | 42.9% | 0.066(0.44%) | 333 | 0.221 | 0.07/**1.94**/0.78 | ✗ 密度变差 |
| 4 | 77.5°+光顺+重打包 | 40.3% | 0.020(0.04%) | 445 | 0.109 | 0.01/0.95/**1.04** | ✗ 新增碎岛 |
| 5 | 71.8°+光顺+重打包 | 40.6% | 0.023(0.05%) | 420 | 0.108 | 0.01/0.95/0.98 | ✓（U 较低未选） |

**结果：利用率 30.9% → 41.6%（+10.7pp）**，大面积空块 0.238→0.020 消失；
同轮实测也说明"岛越少越好/角度越高越好"均不成立（89°密度崩、77.5°碎岛增），
**必须以实测三项比值择优**。视觉复核：岛形态不变、仅重排/旋转、无重叠、无越界。

## 位置与下游

- 脚本：`演示版_终端控制端/03自动UV/scripts/03_auto_uv.py`（v3；交付版已同步）
- 前后布局图：`03自动UV/输出/03_uv_layout_before.png / _after.png / _对比.png`
- 下游刷新（同 UV 链一致）：04烘焙（溢出 12265→7797px，36.4% 清除）、05 提取/标准化/重定向
  ALL PASS、06 GLB（比例 0.00mm / 动画签名 0.11mm）。
- 控制台：`03` 命令 = 本算法；完成标记 `UV_DONE`；摘要行自动显示选定参数与三项比值。
""")
print("  ✓ 重写 03记录(v3)")

# ---------- 2. 根README 结论13 更新为 v3 描述 ----------
sub(os.path.join(MD, "README.md"), [
 ("13. **UV浪费检测与参数寻优 (03, 2026-09-17)**: 展开后自动测量 利用率/最大空方块/岛统计/密度CV；**判据自参照**（最大空方块面积≥最大岛面积=大面积浪费）→ 参数阶梯(66°/75°/82°/89° × 均匀化+CONCAVE重打包)自动重跑选优；本模型 利用率 30.9%→41.8%、空块消失，全链已刷新",
  "13. **UV浪费检测与自适应搜索 (03, 2026-09-17 v3)**: 展开后自动测量 利用率/最大空方块/岛统计/密度CV；**判据自参照三项比值**(废料比<1 / 密度比≤1 / 岛数比≤1, 无模型相关常量, 换模型仍成立)；5轮预算自适应二分(朝合法峰)，跑不好取最好一版不阻断；定状态复测不过依次退用次优；本模型 利用率 30.9%→41.6%、空块消失，全链已刷新"),
])

# ---------- 3. 07 行 & 复盘数字 ----------
sub(os.path.join(MD, "v3_QuadRemesher/07管线集成/README.md"), [
 ("| 03 | ~1分钟 | Smart UV + 浪费检测/参数寻优（利用率30.9%→41.8%，空块消失） |",
  "| 03 | ~1分钟 | Smart UV + 浪费检测/自适应5轮搜索（利用率30.9%→41.6%，空块消失） |"),
])
sub(os.path.join(MD, "项目问题复盘总记录.md"), [
 ("本模型 30.9%→41.8%、空块 0.238→0.020",
  "本模型 30.9%→41.6%、空块 0.238→0.020（v3 自适应5轮+三项比值择优）"),
])

# ---------- 4. 交付版同步 ----------
for src, dst in [
 (os.path.join(DEMO, "03自动UV/scripts/03_auto_uv.py"),
  os.path.join(DELV, "03自动UV/scripts/03_auto_uv.py")),
 (doc, os.path.join(DELV, "03自动UV/UV空间浪费检测与参数寻优_20260917.md")),
]:
    if os.path.exists(dst) and filecmp.cmp(src, dst, shallow=False):
        print("  = 已同", os.path.basename(dst))
    else:
        shutil.copy2(src, dst); print("  ✓ 同步交付版", os.path.basename(dst))
print("完成")
