# 演示版_终端控制端 与 v3交付/方案记录 的同步记录（2026-09-17）

## 背景

`演示版_终端控制端` 是最初从 v3 方案独立出来的录屏演示版；08月底~09-17 的全部迭代
（01a眼窝、02碗、04贴图处理、05绑定/标准化/重定向、**新增06 GLB**、控制台管线）
都在演示版上进行，`v3_QuadRemesher_交付/` 与部分方案md 停留在 08月状态 → 本次全面检查与同步。

## 一、脚本同步（演示版 → 交付版，共 新增12 / 更新21 文件）

| 环节 | 同步内容 |
|---|---|
| 01 | 4 脚本核对一致（无需变更） |
| 01a | 更新10（place/read_markers、run_eye_socket、socket_ops、eye_socket_config、eyeball_config、run_eyeball_v2、mirror_markers、eye002_*）；新增4（assign_socket_material、face_orientation_close、graze_angle_check、setup_marker_gui）；3ddfa 三 json 复制到交付版同级（脚本数据依赖） |
| 02 | 更新 02_qr_auto.py；新增 **02qr_socket_cup.py**（眼窝碗·程序化） |
| 03 | 更新 03_auto_uv.py |
| 04 | 更新 04_bake.py（含自动贴图处理）；新增 **texture_fix.py**（溢出v1+异常斑v2） |
| 05 | 更新7（retarget/qa_rig/qa_walk/run_all/step1/step2/step3_to_7）+ 点位指南；新增 **normalize_to_tpose.py**（03B标准化）；新增 手动工具 |
| 06 | 新增 **06_output_glb.py**（GLB导出+三段自查） |

同步后复验：所有同名文件哈希一致（无"内容不同"项）。

**未同步项（有意保留）**：交付版各 stage 的 models/截图/历史脚本、05 的 _工作区_过程文件、交付版特有的一次性诊断脚本 —— 属历史归档，与当前实现无冲突。
**说明**：部分演示版脚本内含本机绝对路径（指向演示版数据），交付版照搬后运行时读取演示版数据，属预期（同一台机器）。

## 二、方案md 更新

- 根 `README.md` 索引：v3 状态与关键结论更新至 09-17
- `06GLB导出/README.md`：占位符 → 完整方案与实现文档
- `07管线集成/README.md`：占位符 → 演示版终端控制台文档
- `04纹理烘焙/纹理烘焙_完成档案.md`：追加 09-17 贴图溢出/异常斑自动化处理
- `01A眼窝与眼球/01A完整记录_合并版.md`：顶部变更说明补 09-17 状态
- `项目问题复盘总记录.md`：追加 09-17 复盘（03B负优化转折/眼睑权重滞后/贴图异常斑/重定向判据/GLB导出坑）
- `05骨骼绑定/Mixamo骨骼绑定方案.md`：追加 rest标准化最终策略（仅四肢）
- `_方案与WBS/技术方案_全自动化版v3.md`：补"实现现状"注记（01~06 全链已实现）

## 三、skill 同步

`bash skill包/sync_skills.sh out`：Hermes 技能目录 → 仓库 skill包（6个技能，267 文件）。
其中本次迭代涉及的技能更新：
- `mlops/blender-digital-human-pipeline`：rest标准化（仅四肢；Mixamo Neck/Head 前移31.4mm实测；肩/躯干负优化）、眼区权重统一（区域统计盲区）、**GLB导出验证**（新增 references）
- `3d/blender-uv-texture-baking`：贴图溢出/异常斑两步法
- `3d/glb-inspect-and-report` 等其余按原样镜像

## 四、未动项（有意保留，待用户定夺）

- 交付版 models/贴图等二进制产物（旧交付快照保持原样；测试期产物不进交付）
- `全流程文档/` 下的 docx（对外文档，另行处理）
- 根目录 `v3_MixamoSkeleton_骨骼绑定方案.md` 与 `05骨骼绑定/Mixamo骨骼绑定方案.md` 两份并存（内容有差异，未动）
